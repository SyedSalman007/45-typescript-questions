export {};

let pizza_names:string[] = ['chicken tikka', 'cheese', 'behari boti'];

for(let i = 0; i < pizza_names.length; i++)
{
    console.log(`\n${pizza_names[i]}`);
}

// modified for loop

for(let i = 0; i < pizza_names.length; i++)
{
    console.log(`\nI like ${pizza_names[i]} pizza.`);
}

console.log("\nI really love pizza");